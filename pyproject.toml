[project]
name = "github-activity"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer>=0.26.8",
]

[project.scripts]
github-activity = "github_activity.main:app"

[tool.uv]
package = true